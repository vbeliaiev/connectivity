<% content_for :title, "Modification de l'adhérent" %>

<div class="w-full">
  <h1 class="font-bold text-2xl mt-2 mb-6">Modification de l'adhérent</h1>

  <%= render "form", user: @user, cancel_path: users_path %>
</div>
