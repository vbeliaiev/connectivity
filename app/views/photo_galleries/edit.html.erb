<% content_for :title, "Modification de la galerie photo" %>

<div class="w-full p-3 border border-gray-400 rounded-md">
  <h1 class="font-bold text-2xl">Modification de "<%= @photo_gallery.title %>"</h1>

  <%= render "form", photo_gallery: @photo_gallery, url: photo_gallery_path(@photo_gallery), method: :patch, cancel_path: photo_gallery_path(@photo_gallery) %>
</div>
