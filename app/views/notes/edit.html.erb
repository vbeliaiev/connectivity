<% content_for :title, "Editing note" %>

<div class="w-full">
  <h1 class="font-bold text-2xl">Do you want to edit the note?</h1>

  <%= render "form", note: @note %>
  <%= link_to "Cancel ", note_path(@note), class: "w-full sm:w-auto text-center mt-2 sm:mt-0 sm:ml-2 rounded-md px-3.5 py-2.5 bg-gray-100 hover:bg-gray-50 inline-block font-medium" %>
</div>
