<% content_for :title, "Modification du modèle" %>

<div class="w-full">
  <h1 class="font-bold text-2xl mt-2 mb-6 text-center">Modification de "<%= @catalog_item.title %>"</h1>

  <%= render "form", catalog_item: @catalog_item, cancel_path: catalog_item_path(@catalog_item) %>
</div>
